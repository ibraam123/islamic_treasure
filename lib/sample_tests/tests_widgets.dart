import 'package:flutter/material.dart';


class TestsWidgets extends StatelessWidget {
  const TestsWidgets({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Hello World'),
      ),  
    );
  }
}
